# Copy to mobile/.env
# Simulator/device on the same machine as the API:
EXPO_PUBLIC_API_URL=http://localhost:8000/api
# Physical device on the same Wi-Fi — use your Mac's LAN IP instead:
# EXPO_PUBLIC_API_URL=http://192.168.1.10:8000/api
